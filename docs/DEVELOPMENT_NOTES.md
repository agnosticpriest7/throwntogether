# Vertical Slice #1 — first service

The authored `Assets/Scenes/RestaurantDevelopment.unity` scene is the Web entry in `build-config.json`. Bootstrap remains unchanged. Greybox only: one chef, one seated customer and one Fries order.

## Controls and complete loop

- Move: left stick, WASD or arrow keys. Movement is screen-aligned with immediate stopping; CharacterController prevents walking through counters.
- Use/pick up/place: controller south button (Xbox A), E or Space. Face a nearby station; its label highlights and the bottom prompt describes the action.
- Restart the slice: R or controller Start. This resets the entire scene; it is a prototype retry control, not a save system.

Take a potato from the back-left supply. Place it at the central prep island; wait 1.5 seconds and take the cut potato. Place that in the fryer; wait 5 seconds and take the fries. Put fries on the plating counter, take a clean plate from the front-left source, then interact with the fries to plate them. Carry the plated fries to service pickup. The dish automatically transfers to the customer after a short delay; the customer eats and the HUD confirms completion. Plating also works with a plate on the counter and fries in your hands. You can combine an empty plate with ready fries directly at the fryer. Spare and plating counters provide temporary item storage.

## Small reusable foundations

- `ChefController` handles CharacterController movement, proximity/facing selection and interaction. `ChefInput` owns a per-chef Input System action map with optional device binding. No global single-player input singleton; a later join flow can assign devices to separate chef instances.
- `CarrySlot` enforces one item and transfers ownership without duplicating it. `Carryable` displays an `ItemPayload`; raw potato, cut strips, golden fries and plates use different primitive visuals.
- `IngredientDefinition`, `ProcessingRecipe` and `DishRecipe` ScriptableObjects live in `Assets/Data/VerticalSlice`. Prep and fryer share timed `ProcessingStation` logic driven by separate recipe assets. Raw potato is rejected by the fryer; busy stations cannot release their contents.
- `SourceStation` supplies items, currently unlimited. Its supply policy is the future inventory boundary. `CounterStation` supports pickup/place and combining cooked food with a clean plate.
- `ServiceStation` validates `CustomerOrder` against a dish recipe and performs a short placeholder delivery. The order progresses Waiting → Delivering → Eating → Complete and rejects duplicate service. Employee/server AI is absent.
- Chef and carryable prefabs live in `Assets/Prefabs/VerticalSlice`; materials are shared assets. The authored scene contains layout and component references, not a runtime monolithic scene generator. The Editor creation tool is guarded against overwriting an existing restaurant scene.
- Minimal HUD shows order status, nearby station labels/progress, contextual action, carried item and controls. No management UI.

## Known limits

One player/order/recipe only. No throwing, burning, purchases, finite inventory, persistence, dirty dishes, money, reputation or employees. Automatic delivery is an explicit placeholder. Controls have not been physically tested on an Xbox controller or Xbox Edge. TV/couch readability and movement feel require human feedback. Restart discards all slice progress. The shared Input System maps permit future pairing but Player 2 gameplay is not implemented. The infrastructure-era URP warning remains parked unless visible rendering is affected.

## Automated and Editor validation

Live Editor tests: EditMode 3/3 and PlayMode 4/4 passed, including the existing smoke tests. Coverage includes authored recipe progression and timing, raw-potato rejection by the fryer, plate requirements, single-item pickup/place, timed processing, delivery/completion, movement/stopping/range, and synthetic Input System gamepad movement plus south-button pickup. The synthetic gamepad test is not physical controller validation.

The full loop was also exercised through Unity MCP in Play Mode with the chef positioned at each station: actual station interactions and timers produced plated Fries, delivery, eating and Complete. The running scene and visible success HUD were captured and inspected. No new Console errors remained after these checks. The deployment pipeline reruns both suites on the committed snapshot before building and publishing; build provenance remains available at build-info.json.

Batch validation exposed Unity 6000.6 compiler helpers retaining response-file handles between processes. The pipeline contains each batch Editor and its descendants in a Windows job, releases that job when the stage exits, and regenerates only Builds/Workspace/Library/Bee/artifacts/rsp before each stage, retaining larger caches. Synthetic input explicitly advances input processing and the production input handler with fixed steps, restoring its temporary focus settings afterward.


Final committed-snapshot validation passed EditMode 3/3 and PlayMode 4/4, then built Web successfully from `8ee1b2d399a1ffff293d7f55e7f6f99da9f4ef25`. Deployment commit `de36ced8aa5c5b588f7756b37d021d6dfff3ed9d` on gh-pages completed successfully. The public restaurant rendered correctly; loader, framework, data and WebAssembly assets returned HTTP 200 below /throwntogether/, with application/wasm for WebAssembly. Browser Console showed only the previously parked URP upscaling warning and no errors. No physical controller, Xbox Edge or TV testing was performed. Refresh with a build query if an already-open tab retains the older Bootstrap page.
## Exact human playtest checklist

1. Does movement feel responsive?
2. Is the elevated camera comfortable on a TV?
3. Is it obvious where potatoes come from?
4. Is pickup/place interaction intuitive?
5. Does prepping feel responsive?
6. Is fryer progress readable?
7. Is plating understandable?
8. Is service pickup obvious?
9. Is the customer/order readable from couch distance?
10. Does the complete potato-to-fries loop feel satisfying?

Also try raw potato at the fryer, unplated fries at service, interacting while hands are full, and restarting after completion. Record actual platform, input and findings in PLAYTEST_FEEDBACK.md. The public URL is https://agnosticpriest7.github.io/throwntogether/.

## Safe foundation follow-up

The data and diagnostics foundations preserve the first-service gameplay. See TECHNICAL_ARCHITECTURE.md for the six definition families, compatibility migration and proposed optional Player 2 coordinator. No second chef, new live recipe, progression system or Vertical Slice #2 was added.

Diagnostics: the detailed overlay starts OFF. Toggle with F3 or click the tiny DEV/version strip at the bottom right. The strip shows the deployed commit and detected gamepad count; the expanded panel includes commit/time, active input device, gamepad names/IDs, target, held item/state and FPS. Gameplay mappings remain WASD/arrows/left stick, E/Space/A and R/Start. Diagnostics are compiled out when -developmentDiagnostics is omitted from the direct build invocation.

Additional physical playtest checks: confirm Xbox Edge shows the expected commit; press a controller button if no pad is initially detected; confirm the gamepad count and last-active-device display; disconnect/reconnect the controller and verify keyboard fallback where available; run the existing ten-question checklist above. Neither physical controller, couch readability, Xbox Edge nor second-player behavior is implied by automated tests. No subjective feel changes were made.

Regression additions cover data compatibility and exact times, plating by food state, wrong ingredients, invalid service, restart, and one chef with an assigned gamepad ignoring an unassigned gamepad. The existing full loop remains the acceptance test. Employee capabilities, campaign balance, upgrade effects and future join/restart policies remain deferred design questions, not implemented features.

Live foundation verification: EditMode 8/8 and PlayMode 6/6 passed. Unity MCP exercised the existing timed loop through customer completion with exactly one chef and diagnostics initially closed. Console errors: zero. The committed-snapshot pipeline reruns both suites before publication.

## Web / Xbox Edge readiness audit

Input System 1.20.0 is active (new input backend). Its installed WebGLSupport maps browser devices with mapping="standard" to Gamepad; non-standard devices become Joystick and are not bound to chef controls. The existing left-stick/buttonSouth bindings support the standard Xbox layout. Player 1 currently accepts all mapped pads through the documented solo fallback, not exclusive pairing. No remapping or second player was added. Browser detection can require activating the page and pressing a controller button; a missing pad before interaction is not proof of a hardware failure.

The existing F3/DEV panel now distinguishes browser Gamepad API names/mapping from Unity gamepads, labels each mapped pad's Player 1 eligibility, reports last active device, live canvas/document focus, and last Unity controller connection event. Solo fallback naturally accepts a newly discovered device after reconnection; explicit future device pairing still needs its own reconnect policy. The API can be unavailable/blocked and reports that state without throwing.

Development builds use Assets/WebGLTemplates/Development: a focusable canvas, focused-canvas arrow/Space scroll suppression, focus prompt/button and optional Fullscreen button. A standard pad's A rising edge can focus the canvas only while the page is already active; it cannot activate an inactive browser tab. Web input is disabled without visible-document/canvas focus; held interaction/restart buttons must be released after focus regain. Browser shortcuts, Tab and Escape remain available. No gameplay mapping, movement parameter or processing time was changed.

Resize/fullscreen retain the original 960:600 (16:10) render aspect through letterboxing, including portrait/ultrawide windows. The camera itself is untouched. Unity matches render-target size to canvas CSS size and native devicePixelRatio, preserving high-DPI scaling. The shell does not disable browser zoom. Fullscreen is a user-initiated browser request; unsupported/denied requests show a message rather than failing the game. Omitting -developmentDiagnostics selects Unity's ordinary release template and removes diagnostic UI/helpers.

Hosting remains relative-path HTTPS GitHub Pages with uncompressed hashed data/wasm, no worker threads or cross-origin isolation requirement, and no Unity data cache. The existing URP upscaling warning is parked; new runtime errors are not accepted. Node browser-shell tests run before Unity tests in the pipeline (Node.js 20+ required). Automated tests cover aspect geometry, focus predicates, scroll keys, standard A focus eligibility, Unity focus/input suppression, release-after-focus, and simulated controller removal/replacement. These are synthetic tests, not physical Xbox or Edge certification.

### Short physical Xbox / Edge checklist
1. Refresh and confirm the DEV build ID; focus the page, press A, and open F3/DEV. Record browser controller name/mapping, Unity detection and P1: yes.
2. Complete potato → prep → fryer → plate → customer with left stick/A. Verify arrows/Space do not scroll if testing keyboard fallback.
3. Leave/return to the tab; focus the game, release held buttons and resume. Check Focus: YES and no stuck movement or accidental action.
4. Disconnect/reconnect the controller, press a button, and confirm detection/P1 eligibility and a connection event before resuming.
5. Enter/exit fullscreen, resize/zoom Edge and check the whole room, DEV panel and controls remain visible on the TV. Record console errors and actual Xbox/Edge/controller versions.

Physical controller, Xbox Edge, TV scaling and platform-specific fullscreen permissions remain unverified until that checklist is performed.

References: https://developer.mozilla.org/en-US/docs/Web/API/Gamepad_API/Using_the_Gamepad_API and installed InputSystem/Runtime/Plugins/WebGL/WebGLSupport.cs. Browser API detection and Unity mapping are intentionally reported separately.

Live audit verification: 8 PlayMode tests passed, including synthetic focus/reconnect checks. Unity MCP completed the original timed cooking/delivery loop; Console errors remained zero. The deployment pipeline runs browser-shell tests plus all EditMode/PlayMode tests on committed source before Web publication.

Hosted audit result: source d906a6452a30e33c20c717f59bd6d52a99488d52 passed 4 browser-shell, 8 EditMode and 8 PlayMode tests, built Web and deployed as gh-pages 8a867f555b6b605c51aa1eb46e3446dc5a1c9322. The available in-app browser loaded the restaurant and diagnostics, showed Keyboard as last active input, reflected canvas focus loss/regain, kept scroll offsets at zero after arrow/Space input, and entered/exited development fullscreen. DOM geometry preserved 16:10 at 800x1000 and 1600x800 viewports; the normal high-DPI canvas backing matched the browser scale. Temporary viewport overrides were restored. Visual TV scaling and real Xbox Edge permissions remain physical-playtest items. Hosted loader/framework/data/wasm/helper requests returned HTTP 200 (wasm MIME application/wasm). No browser/runtime or Unity Console errors; only the previously parked URP upscaling warning. No scene, prefab, cooking data or movement-controller changes were made.

## Final pre-playtest foundation batch

Added a reusable five-group audio mixer/prefab, nine cue assets with seventeen original generated placeholder WAVs, and versioned local volume settings. No music, progression save or new gameplay content. F3/DEV retains its original toggle and adds audio sliders plus explicit Save; diagnostics still start closed. Confirm browser audio after a click/key gesture, check pickup/prep/frying/plate/success feedback, mute Master/SFX/UI independently, save and refresh to check persistence. Sound quality/balance and Xbox/TV playback remain human checks; these sounds are placeholders.

Development identity includes 0.1.0-dev, commit and UTC build time. See TECHNICAL_ARCHITECTURE.md for settings migration/protection and Web counter limits. The narrow hygiene review retained authored scene/data/prefab organization, existing source-station supply and automatic service placeholders. New feedback uses explicit scene references and does not select or mutate gameplay outcomes. No speed, camera, range, timing, layout, customer behavior or control changes; Vertical Slice #2 remains unstarted.

New regression coverage checks all five settings values round-trip, invalid/future schemas remain untouched, version 0 migration is explicit, normalized levels map to mixer dB, missing clips are harmless, and every authored exposed volume can be applied/restored. Existing cooking/input/restart coverage remains in the full suites. Performance and final deployment measurements are recorded below after committed-build verification.

Live verification: EditMode 16/16 and PlayMode 10/10 passed. Unity MCP exercised pickup, timed prep, timed frying, plating, delivery and customer Complete; the fryer loop stopped after cooking. Diagnostics started closed, mixer settings applied successfully, and Console errors remained zero. Runtime state was discarded on leaving Play Mode.

Final publication: source b4460f6c92a288e11645aab18ea0c87548272904 passed 4 browser-shell, 16 EditMode and 10 PlayMode tests and deployed as gh-pages 819d6a46fa0b6c2f0db30af6405aafc06497a58c. Public identity and the restaurant/DEV/audio UI were verified. SFX settings survived browser refresh and were restored afterward. Automatic Web filesystem synchronization removes the deprecation warning found during save verification. No new Unity or browser/runtime errors; the existing URP warning remains parked. See [WEB_PERFORMANCE_BASELINE.md](WEB_PERFORMANCE_BASELINE.md) for exact output sizes, approximate 60 FPS and measured load/cache/GC limitations.

Human checks remaining: perform the short Xbox/Edge checklist above, activate browser audio with a click/key if needed, confirm each placeholder cue including sizzle stop, and assess volume/TV audibility. No physical controller or audible balance testing was performed. All foundation work stops here; no Vertical Slice #2 or additional gameplay milestone is started.

## Xbox Edge native game-controls retest

Kyle's physical Xbox test reported Pads: 0, D-pad apparently arriving as arrow keys, no left-stick movement or face-button gamepad input, and inability to invoke the usual hold-Menu → Use game controls path. This is recorded as an unresolved physical compatibility issue, not a mapping defect or a confirmed fix.

Inspection found that Unity's generated loader defaults disabledCanvasEvents to contextmenu and dragstart and calls preventDefault on those canvas events. The development template now overrides that list to dragstart only, preserving the native canvas context menu. This is the strongest plausible interference point, not proof of Xbox causation. Our earlier Focus game/Fullscreen buttons, document capture-phase scroll-key handler and A-button focus polling have also been removed. Canvas focus now follows only a primary pointer click; Menu/right-click, browser shortcuts, focus regain and fullscreen are left to the browser. CSS still fits the original 16:10 canvas without page scrolling. No pointer locking was requested by project code. The loading overlay disappears after initialization and the hint bar sits outside the canvas.

Comparison: NVIDIA's official Edge gamepad guidance tells users to choose Use game controls via the browser controller icon; it does not prescribe keyboard emulation or a page-owned controller-mode switch. The exact hold-Menu route comes from Kyle's working Xbox practice. Unity's loader default can suppress the canvas context menu even when a custom template does not explicitly register that event. Native fullscreen is not established as the cause; removing our helper reduces interference and leaves Edge chrome accessible. See https://nvidia.custhelp.com/app/answers/detail/a_id/5298/kw/display%20drivers%20update and the generated Unity loader's disabledCanvasEvents implementation.

Current retest (supersedes the custom Focus game/Fullscreen instructions above):
1. Refresh and confirm the new DEV build ID. While still in Browsing Controls, click the game once with the browsing pointer, then hold Menu (≡) over the canvas and choose Use game controls. If needed, try over the visible Xbox hint or use Edge's address-bar controller icon where offered.
2. Confirm Pads becomes nonzero, record the browser pad name/mapping and Player 1 eligibility, then test left stick/A. D-pad-as-arrows alone does not confirm gamepad detection.
3. Confirm Edge's Menu UI remains accessible; test using Edge's own fullscreen controls only after mode switching works. Report Xbox/Edge versions and whether the menu differs over canvas versus hint bar if it still fails.

No gameplay input binding, controller-to-keyboard translation, camera, timing, layout or gameplay-feel changes. Native Xbox Game Controls is not considered fixed until another physical Xbox test succeeds. Node regressions execute the actual template configuration and check that context/menu keys remain uncancelled, right-click does not refocus, and no custom fullscreen/gamepad-polling API is needed. The existing pipeline runs both Unity test suites before Web publication.

Hosted refresh verification exposed a stale unversioned web-shell.js cache referring to removed buttons. The template now appends the generated DATA_FILENAME hash to the helper URL, keeping each build's HTML and helper in sync. This is a hosting compatibility correction; no browser event or gameplay mapping is synthesized.

Final Xbox retest candidate: source d6081af0106f9510585db448408c30a9fc6cc6ef passed 5 browser-shell, 16 EditMode and 10 PlayMode tests, built successfully and deployed as gh-pages dd94f9cf7646af912e520f522938c31b2ca2b64a. Pages reports built. The existing in-app browser session now loads and refreshes successfully using the versioned helper, with the Xbox hint visible and custom buttons absent. No new browser/runtime or Unity Console errors; the prior URP warning remains. A right-click attempt in the in-app browser does not expose Xbox's native menu UI to this tooling, so native Menu/Game Controls behavior is not verified. Main is pushed and source changes are limited to the shell, diagnostic wording, tests and documentation. Kyle's physical retest remains required before calling the Xbox issue fixed.

## Xbox A-button follow-up

Physical feedback: joystick now works, but A/other buttons do nothing. An automated gamepad test reproduced a shared focus-release gate blocking A while Menu remained held after focus regain (10/11 passed before the fix; 11/11 afterward). Use and Restart now wait independently for their own release. This is a confirmed software defect, but not yet a confirmed explanation of the Xbox symptom. Bindings, movement and cooking behavior are unchanged.

DEV now shows browser button indices currently down (standard mapping: A=0, Menu=9), Unity A/Menu states, individual release gates and cumulative Use attempts/result. Browser signals are sampled twice per second: hold A briefly when reading them. No browser events are synthesized or intercepted.

Physical retest: refresh to the latest build ID, enable Use game controls, release buttons, face the potato supply and press A. If unsuccessful, open DEV with the browsing pointer, return to Game Controls and report Browser API down indices, Unity A/Menu, release gates, Use attempts/result and Target. Native Menu access must continue working. Physical Xbox verification remains pending.

Verification: source 4a10ffa8e3f81bfc97156528c9c543daa3dc2424 passed 6 browser-shell, 16 EditMode and 11 PlayMode tests and built/deployed as gh-pages dbf2c2bba808b6caccc94dc6bac47e36efc8d146. Pages reports built and the public build-info matches. Available desktop browser loaded the restaurant and expanded DEV fields correctly, with no new runtime errors (the existing URP shader warning remains). Unity Console errors: zero. Main was clean and matched origin/main; legacy branch/tag remain at the final browser snapshot. Physical Xbox causation and A-button success are not verified.

## Retained Xbox interaction evidence
Kyle confirmed pressing/releasing A while Take potato was visible in build 4a10ffa, without pickup. Prior screenshots with Use attempts=2 prove some Use calls reached gameplay, but do not identify the failed potato attempt. No root cause is confirmed. Diagnostic history now retains browser standard A pressed/value/focus evidence, performed Unity Use control/gate state, and the last interaction target/result/held item or exception. Browser sampling runs once per development Update, including with DEV closed; strings refresh twice per second. It is read-only and never focuses the canvas or synthesizes input. Counts reflect observed samples, not guaranteed capture of every browser event. Scene restart/page refresh resets the history.

Next physical test: load the new build, leave DEV closed, enter Game Controls, face POTATOES until Take potato appears, press/release A once, then open DEV and photograph it. No need to hold a button while taking the photo. Native Menu behavior and all gameplay bindings/parameters remain unchanged. Audio decode error from the earlier photo remains unresolved; no claim of an Xbox fix.

Verification: d9dc17bf67442f8be249437a8bb0297f87758e7a passed 6 browser checks, 16 EditMode and 11 PlayMode tests; Web built and deployed as b1cff5ac8dc1401dbeac51b1413f61cbb368dc7c. Public identity matches and Pages reports built. Hosted desktop browser verified a retained keyboard Use signal and Rejected at None with the exact feedback, with no new runtime errors (existing URP warning only). Synthetic controller pickup also exercised the accepted record. This is diagnostic instrumentation, not a confirmed Xbox fix.

## Reserve Menu for Xbox Edge
Physical feedback confirmed holding Menu to reopen DEV immediately restarted the scene. This invalidated the previous retest instructions: Unity counters reset on scene load while the JavaScript A history survived. The screenshot's A samples=18 versus Use signals=0 therefore cannot establish that Unity lost those A presses. Web builds now omit the Start/Menu restart binding; native/Editor Start and keyboard R remain unchanged. DEV provides an explicit pointer-click Restart slice button and the HUD states the Web restart route. A, movement, interaction range, cooking and scene layout are unchanged. Regression coverage exercises browser Menu exclusion, native Menu inclusion and keyboard restart using simulated devices.

Retest: refresh to this candidate, leave DEV closed, enter Game Controls, attempt Take potato, then hold Menu to switch back and open DEV. Scene and evidence should now survive that sequence. The A/pickup issue and earlier audio decode issue remain unconfirmed; no physical fix is claimed.

## Web potato pickup exception
Physical build 38bcf6d evidence: 19 browser A samples, 19 Unity buttonSouth Use signals, 19 attempts and NullReferenceException with POTATOES targeted. This establishes input delivery; previous controller-path hypotheses do not explain this failure.

Inspection found Carryable used CreatePrimitive(Sphere), then unconditionally accessed its Collider. The built Web UnityClassRegistration.cpp includes BoxCollider/MeshFilter/MeshRenderer but no SphereCollider or CapsuleCollider; stripEngineCode is enabled. Unity documents CreatePrimitive's stripped-type dependency. Runtime item visuals now create MeshFilter/MeshRenderer directly from explicitly authored sphere/cube/cylinder mesh and Lit shader references on the Carryable prefab. These are the same built-in shapes, transforms and colors; no disposable collider or runtime Shader.Find lookup is required. No input/movement/interaction/timing change. Tests validate prefab dependencies and collider-free sphere pickup alongside the existing full cooking loop. Physical Xbox confirmation remains required.

Verification: old hosted 38bcf6d reproduced with keyboard E at Take potato: browser logged Can't add component because class SphereCollider doesn't exist, then NullReferenceException. New hosted 4a09a5fd39252c98a488e4430c306b993a2f27c0 visibly picked up a raw potato using the same E action, with Hands full and CARRYING: Raw potato. No new browser errors (existing URP warning only). Pipeline passed 17 EditMode, 12 PlayMode and 6 browser checks, built Web and published gh-pages fce87fdbf8eb59f15752a2494fc10c9ad26c30b7. Pages and public identity verified. This directly reproduces and resolves the built-player exception; physical Xbox success remains pending.

## 0.2.0 playtest updates — polish, co-op and restaurant shift

Kyle physically confirmed Xbox controls and the complete first-service loop in 4a09a5f after dismissing the separate audio decoder error. The newly authorized batch supersedes the earlier stop-at-foundations checkpoint.

- Web audio: the build snapshot replaces imported/compressed AudioClip references in cues with the original PCM16 WAV bytes. PcmWave creates non-streaming clips with SetData, bypassing browser compressed-audio decoding. Native clips, mixer routing and variation remain unchanged. Source cue references are restored after building. This targets the reported EncodingError; audible Xbox confirmation is still needed.
- Readability: compact DEV first, detailed view/audio controls on demand; a ground-facing arrow, P1/P2 station badges, per-player held-item/prompt/feedback rows. Speed, camera and interaction reach are unchanged.
- Co-op: one chef remains default. With two pads detected, P1 owns the first pad and keyboard; A on an unassigned second pad joins coral P2. For keyboard + one pad, use the top-right Keyboard P1 + pad P2 button first. Each chef owns a restricted action map. P2 disconnect retains their chef/item and disables their input; A on a replacement pad reclaims that seat. Menu remains browser-owned. Scene restart/mode changes return to solo; P2 presses A to rejoin. A full reload clears diagnostic history. Xbox must expose both pads for two-pad play; that is not yet physically verified.
- Shift: Play restaurant shift switches from practice to a separate scene. Prototype defaults are six tickets (three fries / three fried mushrooms), two active seats, no expiry/failure timer, elapsed completion-time summary. The second pantry and customer table are only in the shift scene. Prep/fryer accept data-defined processes for both ingredients. No cash, inventory purchasing, employees, reputation or persistence progression was added.

Playtest checklist: confirm new build ID; verify no audio popup and audible cues; check facing/target clarity; join P2, alternate stations, attempt simultaneous use, disconnect/reconnect while holding food; complete six tickets solo and co-op; inspect the summary and restart. Assess kitchen crowding and HUD readability physically before any movement/camera tuning.

Verification: final source 34b17dd755c9dd41301d58d1ad498a80b74b5af4 passed 19 EditMode, 15 PlayMode and 6 browser-shell tests. Unity MCP exercised the mushroom pickup/prep/fryer/plating/service loop with normal timers; the six-order solo shift and controller ownership/reconnection passed automated coverage. Unity Console errors: zero. Web built and published as gh-pages cf9f156f1f83a661fceb1926048c9b25f8ce1000; Pages reports built and hosted build-info matches. The available desktop browser verified practice/shift switching and corrected station labels. The preceding edc3444 build (identical runtime except label width) also verified raw-mushroom pickup and compact DEV with no decoder popup or browser errors. The existing URP upscaling shader warning remains. No physical Xbox audio or two-pad test was performed for this batch.

Web baseline: 47,961,068 deployable bytes (45.7 MiB, eight static files); one desktop hosted reload reached loader-ready in 8,602 ms, with roughly 60 FPS observed during the previous identical-runtime interaction check. These are samples, not controlled benchmarks or Xbox measurements. The build's used-asset list contains 17 PCM byte assets and no placeholder WAV AudioClip dependencies. Temporary audio authoring substitutions were restored successfully by original file path/bytes after Unity unloaded asset objects during building. One earlier build stopped on that cleanup defect, and a retry stopped on a Unity compiler-helper pipe failure; neither failed run published. Final pipeline completed successfully. Legacy branch/tag remain at 540af75152c94420d8a88e20c46b50162b4c9493. Final documentation is a separate commit from the deployed source.

## Roomier layout playtest
At Kyle's request, both authored restaurant scenes now have 20% larger floor dimensions and station spacing. Stations/chefs retain their physical size; camera orthographic size is 7.8 instead of 6.5. The shift mushroom pantry is balanced between potatoes and the fryer. Movement, reach, cooking timers and recipes are unchanged. Travel between stations is slightly longer; assess passing space and TV readability in the next physical playtest.

Source 4cbe92cd53604932400e57ba58b5ed923434237f passed 19 EditMode, 15 PlayMode and 6 browser-shell checks. Unity MCP exercised potato pickup, prep, fryer, plating and service through normal timers and confirmed one completed order. Console errors: zero. Web published as gh-pages 50e48a94c915a23c1e1eaee38c7b8737a1a7a527; Pages and public identity verified. Both hosted modes visually checked with no new browser errors (existing URP shader warning only). Main was clean/pushed and legacy refs unchanged; this verification note is a subsequent documentation-only commit. Physical comfort/readability is not claimed verified.

## Approved 0.3.0 weekend-readiness batch
Scope: priorities 1–3 from the budget discussion: controller menus/TV readability; food/cooking/order feedback; co-op feedback and targeted regressions. Priority 4 (shift variety/expanded summary) is deferred for owner review tomorrow. The current room, speed, reach, recipes and timings are unchanged.

Controls: Y/Escape menu, D-pad/stick/arrows navigation, A/Enter select, B/Backspace back. Restart and mode changes ask before discarding progress; Menu remains Edge-owned. Built player opens the menu first; Editor authoring starts unpaused. Text size, contrast, reduced effects and audio are previewed in memory and persisted only with Save settings. The menu displays P1/P2 device ownership and offers keyboard P1 + pad P2 and safe P2 leave. Put food down before leaving; disconnecting retains it.

Visuals: potato eyes and mushroom cap/stem/slices distinguish ingredients; pantry samples, prep blade/fryer steam, percentage progress, dish icons, ticket phases and explicit P1/P2 labels improve readability. Reduced effects hides the new station animations. DEV is hidden while the game menu is open. The mushroom prep/fry/plate/service loop completed through Unity MCP with normal timers; two-player extra-large/high-contrast presentation was reviewed using simulated controllers, not physical devices.

Weekend checklist: refresh to 0.3.0; enable Edge Game Controls; navigate all menus with one pad; cancel a restart while holding food; try largest text/high contrast on TV; save settings and refresh; verify A does not leak from menu to gameplay; join P2, contend for prep, disconnect/reconnect with food, put food down and leave/rejoin; complete a shift. Listen for PCM audio cues and note any popup. Report build ID and the exact failing step.

0.3.0 verification: final source e88c1e537649b03e763c7a5c00b974eac47edabf passed 21 EditMode, 17 PlayMode and 6 browser-shell tests, built successfully and deployed as gh-pages d31291d325bdbfabce7a0b6d975a53920310295c. Pages reports built; hosted build-info matches. The available desktop browser verified menu startup/navigation, mode confirmation and cancellation, confirmed practice-to-shift transition, mushroom pickup, restart cancellation preserving held food, and saved display settings surviving reload. Browser display preferences were restored afterward. Final menu backdrop/ASCII selection marker visually verified. No new browser/runtime errors; the existing URP upscaling shader warning remains. Unity Console errors: zero. Static output: 48,006,254 bytes. Main was clean/pushed and legacy refs unchanged before this documentation-only record.

The first batch test run stopped on headless window focus, before any Web build. The regression now explicitly supplies focused/unfocused conditions while production reads the real focus boundary. Two successful Web candidates were built: the second corrected a Web-font selection glyph and menu contrast found during hosted review. Physical Xbox menu, two-controller behavior, audio and TV comfort remain owner playtests. Priorities 1–3 are complete; priority 4 remains deferred.

## 0.4.0 approved overnight batch — review candidate

Kyle approved the recommended 1–12, 15, 20, 23 and 24 work packages after reporting successful co-op and food-shape testing. This supersedes the earlier shift-variety deferral. No new recipe, layout, movement, camera, reach, timing, customer-rule, economy, employee or throwing changes.

- Removed normal-play world nameplates and their opaque panels. Crates contain recognizable potatoes/mushrooms; prep has a wooden board/knife; fryer has a basket/control back; plates are stacked; the service pass has a shelf/bell. Counter surfaces are ordinary work surfaces. Mint/coral floor borders show P1/P2 targets; small bars and green ready lights show cooking status. Food is larger and plates have a rim. Ticket icons mirror the plated shapes. Customer eyes/arms and state-observing gestures add presentation without changing behavior; reduced effects suppresses motion.
- Plating on either ordinary counter works food-first or plate-first. Finished dish stays on the counter; **press A / E again to pick it up**, then serve. Only cooked food combines. The old dedicated-looking station identity is removed.
- Menu > Shift length and guided practice offers 3, 6 (default), or 12 orders, cycling the existing two-recipe sequence across the existing two seats. No countdown/expiry or score pressure. Results report play time and separate accepted prep/fry/plate/serve actions for each chef.
- Guided full loop and Prep/Frying/Plating/Serving starting points load fresh practice after confirmation. Each starts with the appropriate existing payload and continues through real station rules. Guidance follows P1; no forced control changes. Full free practice remains the default.
- Build artifacts gain a SHA-256 manifest and publication checks. Restore command prepares a historical gh-pages snapshot by default; -Publish appends a restoration commit without rewriting main. Previous owner-tested co-op/food-shape deployment: d31291d325bdbfabce7a0b6d975a53920310295c, source e88c1e537649b03e763c7a5c00b974eac47edabf. See README commands.

Initial Editor regression run stalled because the new fixture suspended Unity's test-host object. Fixture setup now only suspends actual restaurant roots; the rerun passed 23/23 PlayMode tests in 38.64 seconds. No gameplay defect was inferred from the stalled run. Full batch and hosted verification are recorded below when complete.

Tomorrow's physical checklist: refresh to 0.4.0; check station/held-food recognition without labels at TV distance; plate both ways on both counters and pick up the finished dish; try short/long shifts and one practice checkpoint; join P2 and contest a counter; disconnect/reconnect holding food; open/cancel menus; inspect contributions; check largest text/reduced effects. Confirm audio still works without the old decoder popup. No physical Xbox test was performed by the agent.

Live MCP verification completed potato supply -> 1.5-second prep -> 5-second fryer -> food/plate assembly on SPARE COUNTER -> separate finished-plate pickup -> customer service. No timer values were accelerated in this check. One order completed; P1 summary recorded Prep 1 / Fry 1 / Plate 1 / Serve 1. Co-op HUD was visually reviewed with two simulated pads, which were removed afterward. Unity Console errors were zero. Temporary Editor runInBackground was restored to its original false setting after verification.

0.4.0 final verification: source c7e6342e46a5efb573c5ee64156b389d8e2e76d1 passed **22 EditMode, 23 PlayMode and nine browser/artifact checks**. One successful Web build published as gh-pages 0ac5793ef4140394d5ff97ec24a01d7dd9f47c20. GitHub Pages reports built and hosted build-info matches 0.4.0-dev. Browser review verified the startup menu, short-shift selection/confirmation and 0/3 counter, guided full-loop presentation, plating checkpoint with cooked fries, real keyboard placement/plate pickup/assembly/finished-plate pickup on the spare counter, and the results page recording Plate 1. Extra-large/high-contrast guidance fit the desktop viewport; those preview preferences were not saved and were cleared by reload. No new Unity Console or browser/runtime errors; the existing URP warning remains. No physical Xbox/controller verification is claimed for 0.4.0.

The previous deployment restoration command prepared and validated d31291d325bdbfabce7a0b6d975a53920310295c without publishing. During the new publication, local Git's core.autocrlf=true was identified as a manifest risk; conversion was disabled in the isolated publisher before staging. A Git archive of the actual published commit passed manifest validation. Both publisher/restore scripts now explicitly preserve bytes with core.autocrlf=false; this small script correction and final documentation are subsequent to the deployed runtime source and do not require rebuilding identical game code. PowerShell syntax and nine Node checks passed after that correction.

Final static size: 48,043,773 bytes including manifest, about 0.08% above 0.3.0. Observed desktop idle FPS about 60; loader-ready samples 3,007 ms / 377 ms warm; GC unavailable. See WEB_PERFORMANCE_BASELINE.md for limitations. The runtime was exercised through MCP with normal timers. The temporary Editor background setting and simulated pads were restored/removed, and the aborted test's own temporary scene was removed. Work stops here for owner review; no further milestone starts automatically.

## 0.4.1 background music
Owner approved adding First Service and One More Order as background music, starting at 60%. Added an alternating persistent playlist and original OGG delivery, with existing saved Music/Master preferences respected. No other proposed feature batch was started. Physical Xbox check: refresh to 0.4.1, click game if autoplay is blocked, enable Game Controls, listen for music without a decoder popup, adjust Music, and verify a restart does not restart the playlist. Verification results follow after deployment.

0.4.1 verification: source 723311ad969706fc7d27aeb26e1abe7e554cb850 passed 23 EditMode, 23 PlayMode and 13 browser/artifact tests; Web build succeeded and gh-pages 281a1230a5d3049c8243b882ea3e101976730fcb is built on Pages. Both original OGG files are in the verified static artifact (56,245,386 bytes before manifest). Hosted desktop browser confirmed First Service readyState 4, playback after a normal game click, and advancing playback time while the menu is open. Existing saved 100% Music preference remained intact; fresh defaults are 60%. No new Unity Console or browser errors; existing URP warning remains. No physical Xbox audio verification is claimed. The first PlayMode run caught persistent audio surviving tests that remove listeners; headless batch processes now skip starting background music, and the full rerun passed. Unity MCP confirmed a single loaded music voice at 60%; Editor background setting restored. Main is pushed; legacy refs unchanged.

## 0.5.0 — kitchen variety and co-op readiness
Approved batch: two alternative kitchens, one recipe, co-op onboarding, regressions and Web deployment. Throwing and a Windows package remain stretch work and were not started.

- Menu > Kitchen, shift length and practice > Kitchen offers First Service, Prep Island and Split Line. Selection asks before resetting the current session. Applies to both practice and shifts, remains selected across restarts, and defaults to First Service on a fresh launch. The room, camera, speed, reach and existing recipe timings are unchanged. First Service keeps existing station positions; tomato supply is added. Alternatives use the same nine stations and two player spawn anchors with shared routes and no forced two-player dependency.
- Tomato Salad: whole tomato -> prep (1.5 seconds) -> sliced tomato -> combine with a clean plate on an ordinary counter -> pick up -> serve. No frying. Red fruit/green stem and red slices distinguish it from potatoes and mushrooms. Tomato salad joins the authored six-order rotation; 3/6/12-order choices remain. The practice submenu adds a guided salad loop. Existing potato guidance still works.
- Co-op setup and controller help is a dedicated main-menu page: resume/join instructions, keyboard P1 + pad P2, controller P1, safe P2 leave, and Xbox native Game Controls instructions. Disconnect notices distinguish P1/P2; reconnecting P2 retains chef, food and ownership and requires A release before another use. No new controller mappings.
- A focused Editor test run passed all new gameplay checks but exposed music surviving camera/listener replacement in two existing tests. A test-run callback now suspends automatic background music for Editor tests and restores it afterward; headless audio suppression remains.

Review: try all three kitchens solo and co-op; check target recognition and circulation at the central island; complete hot and cold tickets together; plate salad both ways; cancel a kitchen switch while holding food; select a layout and restart; reconnect each controller with food held; verify background music continues through menus and layout changes. Physical Xbox results remain Kyle's testing.

Pre-build verification: 25/25 live Editor PlayMode tests passed after the test-music isolation correction. Capsule-grid checks found connected P1/P2 spawns and reachable stations in all three layouts. Unity MCP completed normal-timer tomato prep -> counter plate assembly -> pickup -> customer completion in Prep Island, and the original potato prep/fry/plate/serve loop in Split Line. Both alternative layouts were camera-reviewed; Unity Console errors zero. Editor runInBackground restored to false after leaving Play Mode. A scene safety backup was compared byte-for-byte before authoring after inspection tracking marked the scene dirty; no unsaved user edits were discarded.

0.5.0 final verification: source d080241835b3b35957c79ed607f6583d3ec87bb1 passed 26 EditMode, 25 PlayMode and 13 browser/artifact checks. One Web build succeeded; gh-pages 1717dc350081e42666ccc6be94190b4dfaf03040 is built on GitHub Pages. Hosted build identity matches 0.5.0-dev. Browser review verified all three kitchen choices, reset confirmation, Split Line loading, the dedicated co-op help page, and Tomato Salad guided practice retaining the selected kitchen with the correct ticket/source hint. Background music continued across the kitchen switch. No new Unity Console or browser/runtime errors; the existing URP upscaling warning remains. Static artifact: 56,262,024 bytes before manifest. Main is pushed/clean and legacy branch/tag unchanged. Documentation verification is committed separately from the deployed runtime. Physical Xbox/co-op comfort, reconnect behavior and music remain owner checks; this batch is complete and no stretch milestone starts automatically.

## 0.5.1 main menu and recipe book
Requested opening flow: Play a level / Practice -> First Service, Prep Island, or Split Line. Shift length remains selectable. Return to main menu confirms leaving the current level; the next choice starts fresh. Y / Escape opens the in-game pause menu, with Recipe book immediately below Resume. The book covers fries, fried mushrooms and tomato salad using current recipe data, including plating on any ordinary counter and picking up the completed dish. It is also available before choosing a level. Editor Play remains unpaused for authoring.

Physical Xbox follow-up: navigate the opening menu with stick/D-pad and A; select a kitchen; open Recipe book with Y then A; check TV readability and B/back; resume with held food unchanged. Edge's three-line Menu remains browser-owned. Verification follows below.

0.5.1 verification: source f714a78d235f74f14ca419efc654218a91d8d71a passed 28 EditMode, 27 PlayMode and 13 browser/artifact tests. Web build succeeded and published as gh-pages 9dac6a4732b0029d1c199014c0e3fd1e3ec0030d; live build-info reports 0.5.1-dev. Hosted browser review confirmed the opening menu, all three level choices, Prep Island shift loading, pause-menu recipe pages for all three dishes (salad correctly omits frying), B/back navigation, and confirmation before returning to Title. Browser error/warning query returned none. Live Unity MCP verified frontend blocking, fresh shift loading, and held-item preservation through recipe-book pause/resume; Console errors zero. Static artifact is 56,271,822 bytes before manifest. Main is pushed/clean and legacy branch/tag unchanged. The initial live regression run exposed an old menu-row expectation; it was updated for Recipe Book, retaining restart confirmation coverage, before the final fully passing pipeline. Physical Xbox/TV review remains the owner follow-up above. No additional milestone started.

## 0.6.0 dishwashing, hands-on cutting, Garden Salad, and mode placeholders
Owner reports 40% remaining and authorizes this feature batch. Use at the prep board starts cutting; remain still. Movement stalls progress; press Use again to resume. Another empty-handed chef can take over after the first leaves. Frying remains unattended. Finished meals return dirty plates to a kitchen rack: pick one up, Use at the sink, remain still for 3 seconds, then pick up the clean plate. Existing clean-plate supply remains unlimited; washing is a reusable foundation, not a new shortage/economy system.

Garden Salad requires sliced tomato AND chopped lettuce on one clean plate, in either order. Partial salad plates cannot serve a ticket. Use ordinary counters for assembly/storage; pick up the finished plate separately. Updated recipe-book instructions and guided salad practice. Added lettuce crate/sink/return rack to all three layouts without changing room, camera, movement or interaction range.

Opening modes: Tutorial (grey), Quick Play (current levels), Career (grey), Trials (grey), Endless (grey). Physical Xbox follow-up: interrupted cutting/washing and co-op takeover; identify lettuce, dirty plates and sink from the couch; prepare Garden Salad in either order; navigate greyed-out modes; confirm Menu still belongs to Edge. No physical test is claimed. Verification follows below.

Live validation before build: normal timers completed tomato prep, lettuce prep, two-component counter assembly, customer delivery/eating, dirty-plate return, washing and clean-plate pickup through Unity MCP. Partial salad did not match the order. New crate/sink positions were adjusted after camera review to avoid the customer table and floor boundary. An inspection command assumed the wash had finished while its artificially positioned chef was still interrupted by collision; checked the actual state, resumed from a clear side, and verified Clean plate. Unity Console remained empty. Final regression/build results follow.

Hosted review caught return-rack overlap with the lower customer table (not present in the single-seat practice review). Moved the new racks left in all layouts, preserving customer tables and all original stations; reran verification before final publication.

0.6.0 final verification: source af5077a037e67a912b196e98eda398e640bb219c passed 29 EditMode, 31 PlayMode and 13 browser/artifact checks. Web build succeeded (56,292,863 bytes before manifest) and published as gh-pages 9b4f2ac71ce273f360f72057b9ba3a536f526815. Live build-info matches 0.6.0-dev and the source commit. Hosted review confirmed Quick Play, disabled Career activation, Garden Salad instructions with both ingredients, scene loading, and final return-rack clearance from the second dining table. No browser errors; the existing URP upscaling warning remains. Unity Console errors zero. Live MCP had already completed Garden Salad preparation/assembly/service and dirty-to-clean washing with normal timers. Final layout access and all existing loops passed after rack correction. One old remote-range test location was moved outside the kitchen after the new sink legitimately entered its range; interaction range remains unchanged. Main pushed/clean, origin correct, legacy branch/tag untouched. The owner-reported 40% remaining is recorded without inferring current usage. Stop here for physical Xbox/co-op/readability review.

## 0.7.0 — approved chef integration
Reviewed and preserved the character work from the separate handoff. Quick Play > kitchen > Your chef now offers Slim, Standard and Fuller bodies, clothing/colors, independent faces, hair, hats and glasses, with separate Player 1/2 selections and a live 3D preview. Ready starts cooking; pause > Choose your chef reopens the wardrobe. Appearance persists across kitchen loads and co-op rejoining within this application session, not browser restarts. Models/poses remain prototypes; gameplay settings and controls are unchanged.

Local candidate browser review verified the preview, build changes, independent P2 selection and P1 kitchen appearance without browser errors. Final development publication uses the normal Xbox-compatible shell, diagnostics and music rather than the candidate's default Unity template. Final test/build/deployment results follow after verification.

Physical follow-up: navigate the wardrobe with Xbox A/B and stick/D-pad; choose distinct P1/P2 looks; check all three silhouettes and carried food at TV distance; join/rejoin P2 and switch kitchens. No new physical controller test is claimed.

0.7.0 final verification: source 944bc4253d99055cdaccadc6cd465902861564a2 passed 34 EditMode, 33 PlayMode and 13 browser/artifact checks. Unity MCP verified modular-chef potato pickup, normal-timer prep/frying, counter plating and accepted service; Console errors zero. Web build succeeded, and the exact committed deployment archive passed manifest verification (56,888,417 bytes before manifest). gh-pages 05f678740455ec1bcc6073db1ac89afa0492392f is built on Pages; public build-info matches 0.7.0-dev and source. Hosted desktop review confirmed kitchen selection, wardrobe rendering, chef entering the kitchen, pause-menu wardrobe reopening, and playing OGG music. No browser errors; the existing URP upscaling warning remains. Main was clean after publication and legacy refs unchanged. This verification note is a separate documentation commit from the deployed runtime. Physical Xbox/controller and couch readability remain owner review.

## 0.8.0 — approved kitchen visual direction
Kyle approved the isolated visual study and requested proceeding. Integrated its warm wood/cool commercial materials, simple beveled modules, clean tiled kitchen floor, warm dining floor, short walls, soft light and 55-degree orthographic angle into the existing playable kitchens. Existing layouts, scale, collisions, carry anchors, controller mappings, cooking/washing durations and customer behavior are preserved. Production sink omits the review-only sample plate. Existing customers and food shapes remain; this is not another gameplay milestone.

Authoring verified unchanged original collider properties and transforms before saving both scenes. Unity MCP camera review covered all three layouts with two chefs using a temporary simulated gamepad (removed afterward). The fries pickup/prep/fry/plate/service path succeeded using explicit 1.5/5-second processing steps because background Editor time was not advancing normally. This is not a physical controller test. Full suite/build/deployment results follow below.

Physical review: refresh to 0.8.0; compare all three layouts solo and co-op; identify prep/fryer/sink without labels; check carried food/chef visibility and target borders from the couch; complete fries and Garden Salad, then wash a returned plate. The existing large carried food still partly covers faces at this angle; no item rescaling was bundled into this approval.

0.8.0 final verification: source 2924dd379d55923fb63532669885d735443eed16 passed 36 EditMode, 35 PlayMode and 13 browser/artifact checks. Web build succeeded; the exact committed deployment archive passed manifest verification (59,909,011 bytes before manifest). gh-pages 1c1f0df081ca6fd447f0cb745216f38d3ac534a8 is built on Pages, and public build-info matches 0.8.0-dev. Hosted desktop review confirmed the main menu, wardrobe preview, First Service art/HUD clearance, safe kitchen-switch confirmation and restyled Prep Island loading. OGG music played; no browser errors, with the existing URP upscaling warning unchanged. Unity Console errors zero. Main was clean following publication and legacy refs unchanged. Final verification notes are a separate documentation commit from the deployed runtime. Physical Xbox/co-op readability remains Kyle's review; no new gameplay milestone starts automatically.

## 0.8.1 — warm flooring and First Service circuit
Owner requested starting with flooring and rearranging the first level using the supplied illustration as inspiration, not a copy. Kitchen floors now have cream ceramic tiles, a muted terracotta inset border, narrow recessed joints and small bevels; dining floors use staggered warm oak planks. Floor footprints/colliders, camera, chef scale, controls, interaction reach and processing durations are unchanged. The shared flooring appears in all kitchens; only First Service's station/spawn definition changed.

First Service groups the four pantry crates in the back-left, prep and sink on the left, plates and two ordinary assembly counters in a central island, fryer at the back, and service/dirty-dish return along the dining edge. Customer positions and behavior remain unchanged. Other two kitchen definitions remain untouched. No new recipe, appliance behavior or milestone.

Unity MCP was unavailable during this task. Authoring ran through Unity batch mode in an isolated current-source copy, preserving the open Editor session; only intended scenes, floor assets/materials and Kitchen0 were copied back. Immediate batch camera captures needed explicit review-only material color blocks; these are not saved or shipped. Captures: Builds/FloorReview/gameplay.png and closer.png. Existing access tests now choose an unobstructed station side and assert real focus selection, instead of teleporting south into a neighboring counter. Added two-chef capsule-clearance checks on both island aisles. Full committed-snapshot results follow after verification.

Owner playtest: First Service pantry access from the sides; identify ingredients without labels; pass another chef on both sides of the island; prep/fry/assemble/serve a dish; return and wash a dirty plate. Assess travel distances and floor contrast from the TV. No physical Xbox/co-op comfort verification is claimed.

0.8.1 verification: source d9277e2b479f8309db43b515998f94a541cd9022 passed 36 EditMode, 36 PlayMode and 13 browser/artifact tests. Full cooking/shift, real station focus/access, all three layouts and both first-level island passing lanes passed. Web succeeded; exact committed deployment archive verified at 58,167,461 bytes before manifest. gh-pages 7d76d32e797a8a576d107e28468e20dc75add0d3 is built on Pages. Hosted browser review confirmed Quick Play > First Service > wardrobe > new kitchen, correct flooring/material colors, four pantry displays and HUD clearance. Browser errors: none; existing URP upscaling warning unchanged. Unity batch authoring/tests/build succeeded; live Editor Console/MCP verification was unavailable, so no claim of a live MCP cooking run. Physical Xbox/two-controller comfort remains owner review. Legacy branch/tag unchanged. Final notes are a documentation commit after the deployed source.

## 0.8.2 — appliance, counter and pantry refinement
Owner approved priority items 1–3 only. Original Blender modules now provide framed metal cabinets, pull handles, consistent wood worktops, straight/end/corner-cap/steel counter variants, a prep board with resting knife, a twin-basket fryer with recessed wells/dials/vents, and a sink with drainboard/tap/drain. Four shallow wooden crates contain decorative potatoes, mushrooms, tomatoes and lettuce. No new recipes or interaction rules. Dining furniture, wall/exterior art, broader lighting, food-in-hand and HUD changes remain outside this batch.

Blender MCP authored a separate TT_Kitchen_Refinement_v01 scene and restored the open character scene without overwriting its file. Editable source and script: ArtSource/KitchenV2. Eleven FBX/prefab modules and a shared palette live under Assets/Art/Kitchen/*V2. Original visual-study assets remain untouched. Largest module is 9,172 triangles; no textures or per-frame model-generation system added.

Unity MCP remained unavailable. Isolated Unity batch authoring replaced only StationArt visuals in both playable scenes and verified identical original collider serialization and world transforms before saving. Layout definitions, player/carry scale, camera, reach, cooking/washing times and customer logic stayed unchanged. includesPantryDisplay prevents KitchenPresentation from generating duplicate crate/item displays; source stations still create normal held food when used. Review captures: Builds/PropReview/gameplay.png and closer.png. Tests cover bounded module dimensions/geometry, collider-free artwork and four correct authored pantry displays without duplicate Carryable previews. Full build/deployment results follow.

Owner follow-up: identify all four ingredients and prep/fryer/sink from the couch; pick up and prepare food, plate on the island, serve, then wash a returned dish; check held items and two chefs against the new art. Physical Xbox/controller testing is not claimed.

0.8.2 verification: source 149ed65c4c84086b8c0fd3d09751354c0e622b1f passed 37 EditMode, 36 PlayMode and 13 browser/artifact checks (86 total). Web succeeded; the exact committed deployment archive verified at 59,718,020 bytes before manifest. gh-pages 0c851d101de598bda97ab38529789d62d4098179 is built on Pages. Hosted Quick Play > First Service > wardrobe > kitchen review confirmed correct new materials, all four pantry displays without duplicates, appliances/counters and HUD clearance. Browser errors: none; existing URP upscaling warning unchanged. Full cooking and layout regressions passed; no new live Unity MCP or physical-controller verification is claimed. Legacy branch/tag unchanged. Final verification notes are a documentation commit after the deployed source. Stop here for owner visual/TV review.

## 0.8.3 — dining furniture, cutaway room and lighting
Owner approved visual priorities 4–6. Replaced table/chair renderers with original Blender furniture: rounded wood tabletops, aprons/legs and brick-colored upholstered chairs. Existing table collision envelopes, carry-slot heights, customer poses/positions and chair parenting/visibility remain unchanged; customer character/animation polish is still a later priority. The retained chair parenting also retains the existing small eating bob and hides the chair with its customer at completion.

New reusable low wall panels, three back-wall windows and a closed cutaway entrance frame the room. The entrance is decorative: the original boundary collider stays closed; no exit, door interaction or scene transition was added. Foreground/side walls remain low, with the existing camera unchanged. Window panes use opaque illustrated colors rather than transparency/reflections. Two wall sconces and bounded shadowless warm dining lights complement the existing soft-shadow key; slightly cooler/dimmer ambient lighting reduces washout, and metal smoothness was adjusted. No post-processing, extra shadow-casting lights, textures or large decorative pass.

Blender MCP authored ArtSource/RestaurantRoom/restaurant-room-v01.blend independently, restoring the open character scene. build_room.py reuses the versioned kitchen primitive helpers. Seven module prefabs/materials/models live in Assets/Art/RestaurantRoom. Unity MCP remained unavailable; isolated batch authoring verified unchanged original collider serialization/world transforms and all carry-slot positions before saving both playable scenes. Layout assets, player scale, controls, reach and all gameplay timings remain unchanged. Actual camera review: Builds/RoomReview/gameplay.png. New regression covers table surface/footprint, collider-free room art, light budget and preserved customer/chair visibility. Full pipeline results follow.

Owner review: assess table/chair scale and warmth on Xbox/TV, check the back-wall/HUD margin, verify chefs and held dishes remain readable near walls, and complete service/dirty-dish washing. No physical controller verification is claimed. Do not infer an interactive exit from the decorative entrance.

0.8.3 verification: source 53d527d9330a64c04b95ec9acc8e55cf478902b2 passed 37 EditMode, 37 PlayMode and 13 browser/artifact checks (87 total). The new window-count test initially counted nested model transforms as additional instances; corrected to count direct room children, then the full suite passed. Full cooking/shift and all-layout regressions passed. Web succeeded; exact committed deployment archive verified at 60,099,003 bytes before manifest. gh-pages 90cbd23cd36dbf644d40a484012f107bd0dc5ae4 is built on Pages. Hosted Quick Play > First Service > wardrobe > kitchen review confirmed furniture, windows, entrance, warm lighting and correct 0.8.3 identity. Back-wall cap sits immediately below the HUD; no overlap observed at the reviewed browser size. Browser errors: none; existing URP upscaling warning unchanged. Unity batch authoring/tests/build succeeded; live Editor Console/MCP verification remained unavailable. Physical Xbox/controller review is still required. Legacy branch/tag unchanged. Final notes are a documentation commit after the deployed source. Stop here for owner visual approval; priorities 7 onward are not started.

## 0.8.4 — service presentation
Owner approved visual priority 7 only. The serving counter now has a slate-colored landing mat with restrained corner markings, a small brass-colored bell, and a low back rail replacing the former tall procedural shelf. A matching open-front dirty-dish tray has a dark liner and low metal handles. These are original Blender overlays on the existing modular counters, using existing kitchen materials. No nameplates, false decorative dishes, new bell interaction, service rules, sounds, delivery duration or layout changes. The bell remains decorative; existing Use still serves matching plated dishes automatically.

ArtSource/ServicePass contains the editable Blender source, construction script and mesh metrics; two prefabs/models live in Assets/Art/ServicePass. Together they add 3,328 triangles, no textures or materials. StationArt.includesServiceDisplay suppresses duplicate legacy service/rack dressing while retaining target borders. Unity MCP was unresponsive; isolated batch authoring verified original colliders and CarrySlot transforms unchanged before saving both scenes. Camera review: Builds/ServiceReview/gameplay.png and closer.png. The new regression verifies no duplicate dressing/fake food/colliders, bounded art height and preserved targeting. Full tests/build/deployment results follow.

Owner review: identify serving versus dirty-dish return from the couch; serve a matching plate, collect the returned dirty plate and wash it. Check food visibility against the new landing mat. Physical Xbox testing is not claimed. Priorities 8 onward remain deferred.

0.8.4 verification: source ee7371940c093955182a5aabb4bea879ccceb7e0 passed 37 EditMode, 38 PlayMode and 13 browser/artifact checks (88 total), including the full cooking shift and new service-display regression. Web succeeded; exact committed deployment archive verified at 60,280,974 bytes before manifest. gh-pages 12424a39c2c162b2a89a42b808c004359197a132 is built on Pages. Hosted Quick Play > First Service > wardrobe > kitchen review confirmed the new serving mat/bell/low rail and return tray without legacy duplicates, correct colors, unobstructed HUD and 0.8.4 identity. Browser errors: none; existing URP upscaling warning unchanged. Isolated Unity authoring/tests/build succeeded; no live Editor Console/MCP or physical Xbox verification is claimed. Legacy references unchanged. Final notes are a separate documentation commit after the deployed source; stop after priority 7.

## 0.8.5 — held food and plate polish; pause after this batch
Owner approved priority 8, then explicitly requested pausing until more tokens are available. Food visuals now use a consistent 1.12 scale instead of 1.35 (about 17% smaller) in hands and on surfaces. No carry anchor, root scale, reach, controller mapping, recipe, timing, layout or ownership change. A shared shallow ceramic plate mesh supplies a beveled rim and recessed center. Fries are separated/fanned with restrained color variation; mushroom slices use semicircular caps and contrasting stems; cut lettuce has simple veins; tomato slices have seeds and occupy less of a combined Garden Salad so green leaves remain visible. Raw ingredients retain their existing recognizable forms. Dirty plates retain stain marks and existing washing behavior.

AuthorFoodPolish creates two small reusable Unity mesh assets and wires the existing Carryable prefab. Carryable still uses the existing state-driven presentation system; only visual scale/geometry changes. No scene changes or new material/texture assets. Isolated Unity camera captures show two temporary chefs with fries/salad and a mushroom plate on a counter; these review objects are never saved. Captures: Builds/FoodReview/gameplay.png, carried-close.png and mushrooms-counter.png. Front-facing chef's face remains clear; the head still partly occludes a dish when facing away. Carry position/camera were intentionally retained. Unity MCP was unresponsive; isolated Unity review is not physical Xbox verification.

New regression coverage checks compact food footprints across ingredient states, unchanged ownership/anchors during visual refresh, authored plate/cap references, dirty-plate refresh and identical Garden Salad geometry for both assembly orders. Full verification results follow. Owner review: identify each held ingredient/dish at TV distance, check two chefs carrying plates, serve fries/mushrooms/salad and wash a returned dish. After publication, stop work and wait for Kyle to resume; do not start priority 9 or another milestone.

0.8.5 verification: source 35cc288e9660d35adbd4ce1263385aecd77a55c1 passed 37 EditMode, 40 PlayMode and 13 browser/artifact checks (90 total), including full cooking/delivery/washing regressions and both new visual-contract tests. Web succeeded; exact committed deployment archive verified at 60,275,392 bytes before manifest. gh-pages 5f1b803db1dedb3edcec434d2eb2aa48a17e3c7d is built on Pages. Hosted review confirmed main menu, wardrobe, kitchen and Serving practice with the actual plated-fries item rendering, correct 0.8.5 identity and no browser errors. The existing URP upscaling warning remains. Away-facing chef partially occludes the plate as noted above; no carry/camera change was bundled. Unity batch authoring/tests/build succeeded; live Editor Console/MCP and physical Xbox verification were unavailable. Legacy references unchanged. Final notes are a documentation commit after deployed source. DEVELOPMENT IS PAUSED at Kyle's request until he explicitly resumes; priority 9 and later work are not started.

## 0.8.6 candidate — seated customers and order bubbles (9–10)
Kyle explicitly resumed development and approved 9–10. The two existing diners reuse the approved modular character kit as seated, non-playable visuals: peach/navy with glasses and sky-blue/rust with a different face/build. Customer counts/positions, recipe matching, delivery duration, two-second eating duration and dirty-dish behavior remain unchanged. Forward seated legs, waiting/delivery poses and restrained eating arm/head motion stay on the visual rig. Reduced effects disables rhythmic motion. The old whole-root eating bob was removed so the chair colliders remain stationary. Old body/head renderers are hidden; original colliders and table slots remain.

Paper-colored order bubbles beside dining seats show table number, existing dish icon/name and actual TO COOK / ON THE WAY / ENJOYING / SERVED state. No patience timer is introduced. Cards clamp to the HUD safe area, scale with text settings and support high contrast. A compact served/time display replaces the full-width ticket header. Solo interaction feedback is narrower; co-op retains separate P1/P2 feedback. Menus, input and recipes stay unchanged.

The authored CustomerPresentation references a disabled ChefAppearance rig with independent NPC choices. No new character model, texture or material assets, employees or navigation systems. Isolated Unity authoring verified original collider/slot signatures; Unity MCP was unresponsive. Builds/CustomerReview contains actual-camera images. Subsequent immediate batch captures can cache skinning; rig motion is tested separately. New tests cover fixed chairs, preserved order/dirty-dish timing, reduced effects, eating rig motion and safe bubble placement at every text size.

Automatic approval review rejected commit/push/deploy because it required explicit release authorization for this batch. Continue local verification; do not publish without resolving that approval. This candidate is not live. Owner review: seated silhouettes, table/card association, default/large text, high contrast, service-state updates, and solo/co-op prompt clarity. No physical Xbox testing is claimed. Stop after 9–10.

Local verification complete: 39 EditMode, 41 PlayMode and 13 Node checks passed (93 total). Final palette-only Web rebuild succeeded; output on disk is 57.50 MiB. Local HTTP review under /throwntogether/ covered startup, First Service, standard/extra-large text and high contrast. Corrected pale Web state strips to darker colors. Browser logs contained no runtime errors and one URP Edge Adaptive Spatial Upsampling/FSR shader warning (post-processing pass unavailable); rendering settings were not changed. Unity MCP/live Editor Console remained unavailable; isolated batch tests/build are the verification evidence. Physical Xbox/TV review remains outstanding. Source is intentionally uncommitted on main, pending explicit commit/push/deploy authorization; public site and legacy references were not changed.

Kyle explicitly authorized committing, pushing main and deploying 0.8.6 after local review. Proceeding through the established clean-source release pipeline; the preceding approval hold is resolved.

0.8.6 release verified: source feabb9252ee21452f4d2c010ad732d65cdc9c20d; gh-pages 1084891b4a7bd7f3297938f9afce58f5e35d009d. Clean-source pipeline passed 39 EditMode, 41 PlayMode and 13 Node checks; verified 60,291,134 Web bytes. GitHub Pages reports built and public build-info.json matches 0.8.6-dev/source commit. Hosted Unity main menu loaded with no runtime errors; the previously documented URP upscaling warning remains. Legacy branch/tag unchanged. Final verification notes are a documentation-only commit after deployed source; no further development begun.

## 0.8.7 — small success cues
Approved final small batch before owner resets allowance. Brief mint outlines mark actual pickup/place or plate assembly; small geometry-drawn checkmarks mark accepted service, completed attended prep and washing. No labels, particles, new assets or gameplay changes. Cues use scaled time (pause-safe), disappear on disable, and Reduced Effects keeps them static without fading. Existing scene-filtered HUD renders both players' station feedback. Tests cover rejected use, expiry, reduced effects, lifecycle clearing and prep/wash completion. Unity MCP did not respond; isolated pipeline verification follows. Stop after deployment; no automatic allowance reset or further milestone.

0.8.7 release verified: source 6d801c95776676d2a841d0e8f030e06b89b8ed39; deployment c657319. 39 EditMode + 42 PlayMode + 13 Node checks passed (94 total). Generated Web artifacts verified: 60,296,832 bytes. Pages reports built; public build-info matches 0.8.7-dev and hosted Unity menu loaded. No browser runtime errors; the documented URP upscaling shader warning remains. Legacy refs unchanged. Live Unity Console and physical Xbox testing unavailable. These final notes are documentation only after deployed source. Work paused pending owner's next instruction.
